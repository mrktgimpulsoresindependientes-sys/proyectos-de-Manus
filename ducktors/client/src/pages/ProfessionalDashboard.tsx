import { DucktorsPage } from "@/components/DucktorsPage";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { CalendarRange, FilePenLine, Mail, Settings2, UsersRound } from "lucide-react";
import { Link } from "wouter";

export default function ProfessionalDashboard() { const { data, isLoading, error } = trpc.professional.agendaSummary.useQuery(undefined, { retry: false }); const locked = !data?.hasLinkedProfile; return <DucktorsPage><section className="bg-sky-50 py-10"><div className="container"><Link href="/profesionales" className="text-sm font-bold text-cobalt-700">← Volver al espacio profesional</Link><div className="mt-5 flex flex-wrap items-end justify-between gap-5"><div><p className="section-kicker">PANEL PROFESIONAL</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-.05em] text-cobalt-950">Agenda y perfil de consulta</h1></div><Button disabled={locked} className="bg-cobalt-700 font-bold">Agregar horario</Button></div></div></section><section className="container py-10 md:py-14">{error ? <div className="rounded-2xl border border-sky-100 bg-white p-6 text-slate-600">Inicia sesión para acceder a tu agenda profesional. Una vez autenticado, Ducktors validará la vinculación con Yellow Duxn antes de mostrar datos clínicos.</div> : <><div className="rounded-2xl border border-sky-100 bg-white p-5 shadow-sm"><div className="flex gap-3"><Settings2 className="mt-0.5 size-5 shrink-0 text-cobalt-700" /><div><p className="font-bold text-cobalt-950">{isLoading ? "Comprobando tu perfil..." : data?.notice}</p><p className="mt-1 text-sm leading-6 text-slate-600">Los controles se activan únicamente después de validar un perfil y horarios reales; no se crean pacientes, turnos ni disponibilidad de demostración.</p></div></div></div><div className="mt-7 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-[1.5rem] border border-sky-100 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="icon-shell"><CalendarRange className="size-5" /></span><div><h2 className="font-display text-2xl font-bold tracking-[-.04em] text-cobalt-950">Agenda de atención</h2><p className="text-sm text-slate-500">Horarios, pacientes y cambios</p></div></div><div className="mt-6 grid gap-3 md:grid-cols-3"><Metric icon={CalendarRange} label="Horarios disponibles" value="—" /><Metric icon={UsersRound} label="Pacientes programados" value="—" /><Metric icon={Mail} label="Cambios por comunicar" value="—" /></div><div className="mt-6 rounded-xl border border-dashed border-sky-200 bg-sky-50/50 p-6 text-center text-sm leading-6 text-slate-600">No hay una agenda visible hasta vincular el perfil Yellow Duxn. Aquí se mostrarán los turnos, cancelaciones y cambios de forma cronológica.</div></div><div className="rounded-[1.5rem] border border-sky-100 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="icon-shell"><FilePenLine className="size-5" /></span><div><h2 className="font-display text-2xl font-bold tracking-[-.04em] text-cobalt-950">Perfil y SEO</h2><p className="text-sm text-slate-500">Información pública verificable</p></div></div><form className="mt-6 grid gap-4"><Field label="Nombre público" placeholder="Disponible al vincular el perfil" /><Field label="Título SEO" placeholder="Título para resultados de búsqueda" /><div className="grid gap-2"><Label>Descripción SEO</Label><Textarea disabled placeholder="Descripción de la consulta o clínica" className="min-h-24" /></div><Button disabled className="bg-cobalt-700 font-bold">Guardar cambios</Button></form></div></div></>}</section></DucktorsPage>; }
function Metric({ icon: Icon, label, value }: { icon: typeof CalendarRange; label: string; value: string }) { return <div className="rounded-xl bg-sky-50 p-4"><Icon className="size-4 text-cobalt-700" /><p className="mt-4 text-2xl font-bold text-cobalt-950">{value}</p><p className="mt-1 text-xs leading-5 text-slate-500">{label}</p></div>; }
function Field({ label, placeholder }: { label: string; placeholder: string }) { return <div className="grid gap-2"><Label>{label}</Label><Input disabled placeholder={placeholder} /></div>; }
