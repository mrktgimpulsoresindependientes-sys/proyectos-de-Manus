import { DucktorsPage } from "@/components/DucktorsPage";
import { ProfileReviews } from "@/components/ProfileReviews";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CalendarDays, CircleUserRound, MapPin, ShieldCheck, Video } from "lucide-react";
import { Link, useRoute } from "wouter";
export default function ProfessionalProfile() { const [, params] = useRoute("/profesional/:slug"); const { data: provider, isLoading } = trpc.directory.bySlug.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) }); return <DucktorsPage><section className="container py-10 md:py-14"><Link href="/directorio" className="inline-flex items-center gap-2 text-sm font-bold text-cobalt-700"><ArrowLeft className="size-4" />Volver al directorio</Link>{isLoading ? <Skeleton className="mt-8 h-[420px] rounded-[2rem]" /> : provider ? <><div className="mt-8 grid gap-7 lg:grid-cols-[1.15fr_.85fr]"><article className="rounded-[2rem] border border-sky-100 bg-white p-8 shadow-sm"><div className="flex gap-5"><div className="grid size-16 shrink-0 place-items-center rounded-2xl bg-sky-100 text-cobalt-700"><CircleUserRound className="size-7" /></div><div><div className="flex flex-wrap items-center gap-2"><h1 className="font-display text-3xl font-bold tracking-[-.05em] text-cobalt-950">{provider.displayName}</h1>{provider.verificationStatus === "verified" && <span className="verified-pill"><ShieldCheck className="size-3" />Verificado</span>}</div><p className="mt-2 font-semibold text-cobalt-700">{provider.specialties}</p><p className="mt-2 flex items-center gap-1.5 text-sm text-slate-500"><MapPin className="size-4" />{provider.locality ?? provider.city}</p></div></div><div className="mt-8 grid gap-6 border-t border-sky-50 pt-7"><Info label="Sobre el perfil" value={provider.biography || "La información profesional se actualizará desde Yellow Duxn."} /><Info label="Seguros y obras sociales" value={provider.insuranceText || "Coberturas por confirmar con el profesional."} /></div></article><aside className="rounded-[2rem] border border-sky-100 bg-sky-50 p-7"><h2 className="font-display text-2xl font-bold tracking-[-.04em] text-cobalt-950">Disponibilidad</h2><p className="mt-3 text-sm leading-6 text-slate-600">Los horarios y modalidades publicados desde Yellow Duxn aparecerán aquí.</p><div className="mt-6 grid gap-3"><p className="flex items-center gap-2 text-sm font-semibold text-slate-700"><CalendarDays className="size-4 text-cobalt-700" />Cita presencial: {provider.inPersonEnabled ? "por confirmar" : "no disponible"}</p><p className="flex items-center gap-2 text-sm font-semibold text-slate-700"><Video className="size-4 text-cobalt-700" />Videollamada: {provider.telemedicineEnabled ? "por confirmar" : "no disponible"}</p></div><Link href={`/profesional/${provider.slug}/reservar`}><Button className="mt-7 w-full bg-cobalt-700 font-bold hover:bg-cobalt-800">Ver opciones de reserva</Button></Link><p className="mt-3 text-center text-xs leading-5 text-slate-500">La reserva se habilita únicamente con disponibilidad real publicada.</p></aside></div><ProfileReviews slug={provider.slug} professionalId={provider.id} /></> : <div className="mt-8 rounded-[2rem] border border-dashed border-sky-200 bg-sky-50 p-10 text-center"><h1 className="font-display text-3xl font-bold tracking-[-.045em] text-cobalt-950">Este perfil aún no está disponible.</h1><p className="mx-auto mt-3 max-w-xl leading-7 text-slate-600">Puede que el registro esté pendiente de publicación o que la conexión con Yellow Duxn aún no esté activa.</p><Link href="/directorio"><Button className="mt-7 bg-cobalt-700 font-bold hover:bg-cobalt-800">Volver al directorio</Button></Link></div>}</section></DucktorsPage>; }
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-xs font-bold uppercase tracking-[.13em] text-slate-500">{label}</p><p className="mt-2 leading-7 text-slate-700">{value}</p></div>; }
