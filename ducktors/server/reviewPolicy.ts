export type ReviewEligibilityInput = { appointmentState: "requested" | "confirmed" | "cancelled" | "completed" | "no_show"; isAppointmentOwner: boolean; alreadyReviewed: boolean };
export function canSubmitReview(input: ReviewEligibilityInput) { if (!input.isAppointmentOwner) return { allowed: false, reason: "La cita no pertenece al paciente autenticado." }; if (input.appointmentState !== "completed") return { allowed: false, reason: "Las opiniones se habilitan después de una cita completada." }; if (input.alreadyReviewed) return { allowed: false, reason: "Ya existe una opinión asociada a esta cita." }; return { allowed: true, reason: "Puedes compartir tu experiencia de forma respetuosa." }; }
